server:
  port: 0
  output:
    ansi:
      enabled: ALWAYS
spring:
  # DataSource
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/main
    username: dbuserMain
    password: 12345Main
  # JPA
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    ru:
      practicum: INFO
      Zalando:
        logbook: TRACE